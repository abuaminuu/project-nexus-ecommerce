** git flows, payment integration
1- fix after payment, /confirm-payment/ not updating payment object for that order object passed
2- fix update stocks after product paid. done
3- check in dev and prod if refreshing redirect url on GET updates stocks. done
3- add tx_ref to payments(allow null). done
2- searching and sorting done
4- payment provider to send POST requests to /payments/webhook/ ( profile -> payment gateway settings) done
4- db optimization done
5- middlewares done
6- reset password endpoint - achievable
6- signals and advanced orm(q,f params)
6- django redis - part
7- async background tasks/jobs with celery - part
7- django debugger - done
8- graphQL - done
9- passing state with js - done
10- ML integration (recommendation engine)
22- after sucesful auth redirect to /
21- delete item that only belongs to you.
22 - fix payment issues.
33 - after register, rote to loigin to be done by FE
44 - remove endpoint- paymet.. to admin. done
55 - delete/put works on ui not thunder - auth! - Status: 204 No Content solved.
87 - normal vs industry standard of redirecting user eg: if i have endpoint /order/ 
    that may accept id /orders/34/ i dont want users to see 404 by going 
    /orders/user/.... or anyother path that he might try that donot exist on the server done

78 - test all endpoints on external client.

#######
shell - done
gitflow 
cicd pipeline (jenkins/github actions)
containers && container orchestration
IP tracking: security, analytics, audit and monitoring.
system design and components

##### 
AI wih Python - ML integration (recommendation engine)

# deployment 
- pythonanywhere - done
- digital ocean - 
- alibaba cloud
- aws 

#####
General Checklist.
- user model
- signup/login/reset password, profile
- authentication

- other models
- MUST have functionalities
- permissions
- expose API
- CORS

# for testing of db models
# DATABASES = {}

# # for development (comment on dev)
# if "test" in os.sys.argv or "test_coverage" in os.sys.argv:
#     DATABASES["default"] = {
#             "ENGINE": "django.db.backends.postgresql",
#             "NAME": "test_ecommerce_nexus",
#             "USER": "postgres",
#             "PASSWORD": "postgres",
#             "HOST": "localhost",
#             "PORT": "5432",
#     }
# else:
#     # for docker settings env('DB_HOST', 'db'), or 'db'
#     # these are real credentials, use env vars in production
#     DATABASES = {
#         "default": {
#             "ENGINE": "django.db.backends.postgresql",
#             'NAME': env('DB_NAME', 'ecommerce_nexus'),
#             'USER': env('DB_USER', 'nexus_user'),
#             'PASSWORD': env('DB_PASSWORD', 'nexus_pass'),
#             'HOST': env('DB_HOST', 'localhost'),
#             'PORT': env('DB_PORT', '5432'),
#         }
#     }


catch
- POST http://127.0.0.1:8000/api/commerce/v1.1/products/514/
TypeError at /api/commerce/v1.1/products/514/
ProductViewSet.post() got an unexpected keyword argument 'pk'
Request Method:	POST
Request URL:	http://127.0.0.1:8000/api/commerce/v1.1/products/514/
Django Version:
2- General runtime error eg: POST http://127.0.0.1:8000/api/commerce/v1.1/orders/514 and related errors
3- orders/id should not allow PUT on amount, user, (only order metadata details: eg shipping address, promocode, stattus(by admin) etc)
4- expalin : Q,F, Sum Count in django.db.models

    
    # TODO add (users, products, orders, order items, payments) stats 
    # for the last 7 days, 30 days, and 1 year
    # and add privilege check for admin only to access this dashboard view
